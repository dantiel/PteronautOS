name = "pteronautos-build"
main = "src/index.js"
compatibility_date = "2025-01-01"

[assets]
directory = "../webapp"
binding = "ASSETS"

[vars]
GITHUB_REPO = "dantiel/PteronautOS"
GITHUB_REF = "master"
# GITHUB_TOKEN is a secret — set it with:
#   wrangler secret put GITHUB_TOKEN
