name = "build"
main = "src/index.js"
compatibility_date = "2025-01-01"

[vars]
GITHUB_REPO = "dantiel/PteronautOS"
GITHUB_REF = "master"
# GITHUB_TOKEN is a secret — set it with:
#   wrangler secret put GITHUB_TOKEN