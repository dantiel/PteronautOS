// Arduino compiles this library source; the ESP build already compiles the
// canonical table source directly. Both executors use exactly the same tables.
#include <Arduino.h>
#include "MotionWaveformTables.inc"
